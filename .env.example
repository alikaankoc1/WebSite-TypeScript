# Optional: public URL of the contact API (no trailing slash). If unset, Vite dev proxy sends /api to localhost:3001.
# Production example: https://your-backend.example.com
# On the server, set ALLOWED_ORIGINS to include your Netlify site origin (see server/.env.example).
VITE_CONTACT_API_URL=
